2026/01/05 新增

1. 代收商户列表中子功能疑问
   1. 商户名称中的测试用哪个接口
   2. 商户名称中详情用哪个接口
   3. 余额中调额用哪个接口(已添加)
   4. 总预付中明细用哪个接口(已添加)
   5. 剩余预付中明细用哪个接口
   6. 剩余预付中结算用哪个接口
   7. 更多中重置密码用哪个接口(用update)
   8. 更多中重置密钥用哪个接口(已添加)
   9. 更多中登录商户用哪个接口
2. 批量更新产品配置用哪个接口（后期）
3. 批量限额用哪个接口（后期）
4. 批量结算用哪个接口（后期）
5. 对接信息用哪个接口（后期）

2026/01/06 新增

1. 管理员登陆（已接入） 增加IP字段
2. 商户产品绑定保存 id改成product_id
3. 商户产品绑定保存 提交数据改成只提交有变动的数据。API有变化
4. 商户通道绑定保存 提交数据改成只提交有变动的数据。API有变化

2026/01/06 问题反馈

1. 管理员登陆（已接入） 增加IP字段 这个新增的字段有疑问，这个IP是什么IP
2. 商户通道绑定配置（已接入） 现在商户列表中的商户的数据都是空的，如何给这些商户添加数据，不然无法修改和保存
